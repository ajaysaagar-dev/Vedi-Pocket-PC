"""Apps — composition roots for each deployable (agent server, desktop
controller, mobile app, streamer server). All four are Python packages
so they can be invoked via ``python -m apps.<name>.<entry>`` from the
repo root and so that relative imports resolve.
"""
