"""Vedi Pocket PC application package.

Houses the production-grade composition root and the small helpers
shared by every entry point. Anything in this package works the
same way whether it is running from a developer checkout or from a
PyInstaller-frozen ``VediPocketPC.exe``.
"""
