# Runtime deps for the screen-mirror server. The shared domain code
# lives in `../packages/agent-core`; install it editable with:
#     pip install -e ../packages/agent-core
mss
Pillow
aiohttp
pyautogui

# Test deps
pytest
