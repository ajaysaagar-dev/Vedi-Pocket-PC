"""Infrastructure — shared process-level concerns for the Vedi-Pocket-PC monorepo.

Sub-packages:
  networking — mDNS / IP discovery (ServiceAdvertiser, get_local_ip)
  logging    — structured logging configuration
  windows    — Windows adapter re-exports (pyautogui, pycaw, win32)
  config     — environment & runtime configuration helpers
"""
