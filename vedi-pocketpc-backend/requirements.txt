# Runtime deps for the control agent. The shared domain code lives
# in `../packages/agent-core`; install it editable with:
#     pip install -e ../packages/agent-core
fastapi
uvicorn
websockets
pyautogui
zeroconf
pycaw; sys_platform == 'win32'
comtypes; sys_platform == 'win32'
pystray
pillow
qrcode
psutil

# Test deps
pytest
httpx
